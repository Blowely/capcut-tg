# 🎬 CapCut Telegram Mini App - Начало работы

## ✅ Установка завершена!

База данных PostgreSQL успешно настроена и готова к работе.

## 🚀 Быстрый запуск

### 1. Запустите приложение:

```bash
npm run dev
```

Или используйте скрипт:

```bash
./scripts/dev.sh
```

### 2. Откройте в браузере:

- 🎨 **Frontend**: http://localhost:3000
- 🔧 **Backend API**: http://localhost:3001  
- 📚 **Swagger документация**: http://localhost:3001/api

## 📝 Важные заметки

### Настройка Telegram Bot Token

Для работы с Telegram Mini App нужно:

1. Создать бота через [@BotFather](https://t.me/BotFather)
2. Получить токен бота
3. Обновить файл `apps/backend/.env`:
   ```env
   TELEGRAM_BOT_TOKEN=ваш_реальный_токен_здесь
   ```

### Переменные окружения

**Backend** (`apps/backend/.env`):
```env
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/capcut_db?schema=public"
PORT=3001
TELEGRAM_BOT_TOKEN=your_bot_token_here
UPLOAD_DIR=./uploads
TEMP_DIR=./temp
MAX_FILE_SIZE=104857600
```

**Frontend** (`apps/frontend/.env.local`):
```env
NEXT_PUBLIC_API_URL=http://localhost:3000
```

## 🛠️ Полезные команды

### Управление базой данных

```bash
# Prisma Studio (GUI для БД)
npm run db:studio

# Пересоздать схему БД
cd apps/backend && npm run db:push

# Сброс БД
./scripts/reset-db.sh
```

### Разработка

```bash
# Запуск бэкенда отдельно
cd apps/backend && npm run dev

# Запуск фронтенда отдельно  
cd apps/frontend && npm run dev

# Линтинг
npm run lint

# Сборка
npm run build
```

### Docker

```bash
# Остановить PostgreSQL
docker-compose down

# Запустить PostgreSQL
docker-compose up -d

# Логи PostgreSQL
docker-compose logs -f postgres

# Полная очистка (удаляет данные!)
docker-compose down -v
```

## 📱 Тестирование в Telegram

### С помощью ngrok (для локальной разработки)

1. Установите ngrok:
   ```bash
   brew install ngrok  # macOS
   ```

2. Запустите туннель:
   ```bash
   ngrok http 3000
   ```

3. Скопируйте HTTPS URL (например, `https://abc123.ngrok.io`)

4. В [@BotFather](https://t.me/BotFather):
   ```
   /myapps
   → выберите ваше приложение
   → Edit Web App URL
   → вставьте ngrok URL
   ```

5. Откройте бота и тестируйте!

## 📖 Дополнительная документация

- 📘 [README.md](./README.md) - общий обзор проекта
- 🚀 [QUICKSTART.md](./QUICKSTART.md) - быстрый старт
- 🏗️ [ARCHITECTURE.md](./ARCHITECTURE.md) - архитектура проекта
- 🚢 [DEPLOYMENT.md](./DEPLOYMENT.md) - production развертывание
- 🤝 [CONTRIBUTING.md](./CONTRIBUTING.md) - гайд для контрибьюторов
- 📋 [PROJECT_OVERVIEW.md](./PROJECT_OVERVIEW.md) - полный обзор

## 🎯 Первые шаги

1. **Запустите приложение** через `npm run dev`
2. **Откройте http://localhost:3000** в браузере
3. **Создайте тестовый проект**
4. **Загрузите видео** (до 100MB)
5. **Попробуйте редактирование** (текст, фильтры, обрезка)

## 🐛 Проблемы?

### PostgreSQL не запускается
```bash
docker-compose down -v
docker-compose up -d
sleep 5
cd apps/backend && npm run db:push
```

### Порт 3000 занят
```bash
lsof -i :3000
kill -9 <PID>
```

### Ошибки с зависимостями
```bash
rm -rf node_modules apps/*/node_modules
npm install
cd apps/backend && npm install
cd ../frontend && npm install
```

## 💡 Подсказки

- **Swagger API**: http://localhost:3001/api - тестируйте API
- **Prisma Studio**: `npm run db:studio` - просмотр данных БД
- **Hot reload**: изменения применяются автоматически
- **WebSocket**: для real-time прогресса обработки видео

## 🎨 Функции приложения

✅ Аутентификация через Telegram  
✅ Создание неограниченного количества проектов  
✅ Загрузка видео до 100MB  
✅ Timeline редактор  
✅ Добавление текста  
✅ Фильтры (яркость, контраст, насыщенность)  
✅ Добавление музыки  
✅ Экспорт видео  
✅ Real-time прогресс обработки  

## 📞 Поддержка

- 💬 GitHub Issues
- 📧 Email разработчиков
- 📖 Документация в репозитории

---

**Приятной работы! 🚀**

*Если возникли вопросы - см. документацию или создайте Issue.*



